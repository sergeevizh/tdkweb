import { ShowsFilterPipe } from './shows-filter.pipe';

describe('ShowsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
